@model ClipKeep.Models.UserLogin
@{
    ViewBag.Title = "Login";
    Layout = "./_Layout.cshtml";
}

<div id="MainContentLogin">
    <div class="col-md d-flex justify-content-center text-center">
        <img src="~/Icons/check-list128.png" alt="logo " />
        <h2>@Html.Raw(ViewBag.SubText)</h2>

        @using (Html.BeginForm("Login", "Auth", FormMethod.Post, new { @id = "loginform" }))
        {
            <fieldset>
                <div class="editor-group">
                    <div class="editor-label">
                        @Html.LabelFor(userLoginModel => userLoginModel.Username)
                    </div>
                    <div class="editor-field">
                        @Html.EditorFor(userLoginModel => userLoginModel.Username, new { htmlAttributes = new { @class = "form-control", @name = "usernamePropertyName" , placeholder = "Enter Username" } })
                        @Html.ValidationMessageFor(userModel => userModel.Username, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="editor-group">
                    <div class="editor-label">
                        @Html.LabelFor(userLoginModel => userLoginModel.Password)
                    </div>
                    <div class="editor-field">
                        @Html.EditorFor(userLoginModel => userLoginModel.Password, new { htmlAttributes = new { @class = "form-control", @name = "password", placeholder = "Enter Password", @type = "password" } })
                        @Html.ValidationMessageFor(userLoginModel => userLoginModel.Password, "", new { @class = "text-danger" })
                    </div>
                </div>

                <p>
                    <input class="btn btn-primary" type="submit" value="Login" />
                </p>
                <p>
                    Not got an account yet?@Html.ActionLink(" Register!", "Register", "Auth") 
                </p>
            </fieldset>
        }

    </div>
</div>

